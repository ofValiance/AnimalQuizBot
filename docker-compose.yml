services:
  quiz_bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: animal_quiz_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./resources/reviews.json:/app/resources/reviews.json